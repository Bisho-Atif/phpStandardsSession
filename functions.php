<?php

// Declaring a simple function
function myFunction($arg, $anotherArg = '')
{
    // Code
}

// Method Chaining
$object->aFunction($parameter)
    ->anotherFunction($anotherParameter)
    ->theLastFunction($theLastParameter);

// Function with array input
myFunction([
    'element',
    'another element',
]);

// Keep a new line before return statement
public function show($id)
{
    checkForId($id);
    doStuff();
    doMoreStuff();

    return $object;
}